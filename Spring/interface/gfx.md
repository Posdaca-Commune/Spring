# GFX 文件
`*.gfx` 文件用于将图像文件注册到游戏中，并且为其添加一个唯一的名称。<br>
GFX 文件可位于`interface`文件夹中任意位置，包括嵌套的子文件夹。
## 替换顺序
- 当两个不同的图像文件注册了相同的名称时，游戏将使用最后注册的图像文件。
- 当Mod中存在与游戏本体相同名称的图像文件时，游戏将优先使用Mod中的图像文件。
- 当Mod中存在与游戏本体相同名称的GFX文件时，游戏将优先使用Mod中的GFX文件。